import type { Metadata } from 'next'
import WorkClient from './WorkClient'
export const metadata: Metadata = { title:'Work — Aashish Pandey', description:'Video production, ads campaigns, brand content and more.' }
export default function WorkPage() { return <WorkClient /> }
